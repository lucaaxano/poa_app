import UIKit
import WebKit
import Capacitor

class POAViewController: CAPBridgeViewController {
    // Keyboard handling is done by the web layer (ResizeObserver + overflow-y-auto)
    // and Capacitor's Keyboard plugin (resize: 'body').
}
